from django.apps import AppConfig

class SettingsConfigurationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.settings_configurations'
    verbose_name = 'Settings and Configurations'