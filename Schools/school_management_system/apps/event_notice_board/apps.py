from django.apps import AppConfig

class EventNoticeBoardConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.event_notice_board'
    verbose_name = 'Event and Notice Board'